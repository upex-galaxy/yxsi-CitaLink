# =============================================================================
# Supabase Configuration for CitaLink
# =============================================================================
# IMPORTANT:
# 1. Copy this file to a new file named `.env.local`
#    (e.g., `cp .env.example .env.local`)
# 2. Add your actual Supabase credentials to `.env.local`.
# 3. NEVER commit the `.env.local` file to version control.
#
# You can find your credentials in your Supabase project dashboard:
# https://supabase.com/dashboard/project/[YOUR_PROJECT_ID]/settings/api
# =============================================================================

# The public URL for your Supabase project.
NEXT_PUBLIC_SUPABASE_URL=https://your-project-url.supabase.co

# The anonymous public key for your Supabase project.
# This is safe to expose in the browser.
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# The service role secret key for your Supabase project.
# ⚠️ WARNING: This key bypasses all RLS policies.
# It must NEVER be exposed in the browser or client-side code.
# Use it only for server-side operations.
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# The base URL of your application, used for auth redirects.
# Defaults to http://localhost:3000 for local development.
NEXT_PUBLIC_APP_URL=http://localhost:3000

# =============================================================================
# Instructions:
# 1. Rename this file to `.env.local`
# 2. Go to your Supabase dashboard -> Project Settings -> API
# 3. Copy your Project URL, anon key, and service_role key.
# 4. Paste them into `.env.local`.
# 5. Restart your development server.
# =============================================================================